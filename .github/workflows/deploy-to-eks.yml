name: Deploy to EKS 
on:
  push:
    branches: [ main ] 
permissions:
  id-token: write
  contents: read
env:
  EKS_CLUSTER_NAME:  clickshield-eks-cluster
  AWS_REGION: ${{ secrets.AWS_REGION }}
  AWS_ACCOUNT: ${{ secrets.AWS_ACCOUNT_ID }}
  AWS_ACTIONS_ROLE:  ${{ secrets.ACTIONS_IAM_ROLE }}

jobs:
  deploy_to_eks:
    runs-on: ubuntu-latest
    defaults:
      run:
        shell: bash
    steps:
      - name: Set short git commit SHA
        id: commit
        uses: prompt/actions-commit-hash@v4
  
      - name: Check out code
        uses: actions/checkout@v6 
  
      - name: Configure AWS Credentials
        uses: aws-actions/configure-aws-credentials@v6.1.0
        with:
          role-to-assume: ${{ secrets.ACTIONS_IAM_ROLE }}
          aws-region: ${{ secrets.AWS_REGION }}
      
      - name: Login to Amazon ECR
        id: login-ecr
        uses: aws-actions/amazon-ecr-login@v2

      - name: Update kube config 
        run: |
          aws eks update-kubeconfig --name $EKS_CLUSTER_NAME --region $AWS_REGION
  
      # - name: Deploy to EKS 
      #   env:
      #     REGISTRY:  ${{ steps.login-ecr.outputs.registry }} 
      #     REPOSITORY: clickshield-platform-repo
      #     IMAGE_TAG: ${{ steps.commit.outputs.short }}
      #   run: | 
      #     sed -i.bak "s|FRONTEND_IMAGE|$REGISTRY/$REPOSITORY:$IMAGE_TAG|g" manifests/app-deploy.yaml && \
      #     kubectl apply -f manifests/app-deploy.yaml 
      #     kubectl apply -f manifests/app-service.yaml 
